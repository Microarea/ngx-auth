export interface TbAuthEnvironment {
    auth: {
        url: string;
        subs: boolean;
        appid: string;
        redirectUrl: string;
        session: boolean;
        customLogo: string;
    };
}
